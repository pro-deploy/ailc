[toolchain]
# Версия зафиксирована намеренно: канал `stable` воспроизводимости не даёт, поскольку его
# содержимое меняется каждые шесть недель, и сборка выпуска на разных машинах может
# отличаться. Обновление версии выполняется отдельным осознанным коммитом.
channel = "1.93.1"
