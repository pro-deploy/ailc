---
name: code-intel-search
description: "Найти, где в проекте написано про запрошенное: поиск по коду и документации лексическим индексом с оценкой релевантности. Запрос словами в query."
license: Apache-2.0
metadata:
  family: code.intel
  engine: index
  tier: core
  mutates: false
---

# code.intel/search

Найти, где в проекте написано про запрошенное: поиск по коду и документации лексическим индексом с оценкой релевантности. Запрос словами в query.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "code.intel/search", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap code.intel/search <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `code.intel`, движок `index`, тир `core`.
