---
name: spec-questions
description: "Узнать, каких сведений не хватает документам по стандартам, и получить очередь вопросов человеку с черновиками из кода. Задай эти вопросы пользователю, ответы верни через spec/answer."
license: Apache-2.0
metadata:
  family: spec
  engine: generator
  tier: core
  mutates: false
---

# spec/questions

Узнать, каких сведений не хватает документам по стандартам, и получить очередь вопросов человеку с черновиками из кода. Задай эти вопросы пользователю, ответы верни через spec/answer.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "spec/questions", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap spec/questions <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `spec`, движок `generator`, тир `core`.
