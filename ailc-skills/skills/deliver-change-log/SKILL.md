---
name: deliver-change-log
description: "Зарегистрировать текущее изменение в журнале изменений: класс выводится из различия, запись попадает в лист регистрации изменений документов. Намерение правки передавай в query."
license: Apache-2.0
metadata:
  family: deliver
  engine: store
  tier: core
  mutates: true
---

# deliver/change-log

Зарегистрировать текущее изменение в журнале изменений: класс выводится из различия, запись попадает в лист регистрации изменений документов. Намерение правки передавай в query.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "deliver/change-log", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap deliver/change-log <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `deliver`, движок `store`, тир `core`.
