---
name: task-plan
description: "Записать план работ в журнал проекта: по одной задаче на строку. Вызывай СРАЗУ после того, как составил план, до того как приступил к работе: иначе при обрыве сеанса план потеряется вместе с ним."
license: Apache-2.0
metadata:
  family: backlog
  engine: store
  tier: core
  mutates: true
---

# task/plan

Записать план работ в журнал проекта: по одной задаче на строку. Вызывай СРАЗУ после того, как составил план, до того как приступил к работе: иначе при обрыве сеанса план потеряется вместе с ним.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "task/plan", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap task/plan <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `backlog`, движок `store`, тир `core`.
