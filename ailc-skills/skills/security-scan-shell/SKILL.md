---
name: security-scan-shell
description: "Опасные конструкции командных сценариев (расширения sh, bash, zsh): незакавыченная подстановка переменной в аргументе разрушающей команды, исполнение строки через eval, загрузка кода из сети конвейером в оболочку, загрузка с отключённой проверкой сертификата, временный файл по предсказуемому пути вместо mktemp, явное подавление ошибки шага установки или сборки, выдача прав записи всем пользователям. Покрытие паттерновое и построчное: конструкции, разорванные продолжением строки, могут остаться вне охвата, поэтому ручной обзор сценариев развёртывания остаётся желательным."
license: Apache-2.0
metadata:
  family: security
  engine: scan
  tier: core
  mutates: false
---

# security.scan/shell

Опасные конструкции командных сценариев (расширения sh, bash, zsh): незакавыченная подстановка переменной в аргументе разрушающей команды, исполнение строки через eval, загрузка кода из сети конвейером в оболочку, загрузка с отключённой проверкой сертификата, временный файл по предсказуемому пути вместо mktemp, явное подавление ошибки шага установки или сборки, выдача прав записи всем пользователям. Покрытие паттерновое и построчное: конструкции, разорванные продолжением строки, могут остаться вне охвата, поэтому ручной обзор сценариев развёртывания остаётся желательным.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "security.scan/shell", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap security.scan/shell <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `security`, движок `scan`, тир `core`.
