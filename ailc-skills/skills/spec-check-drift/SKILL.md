---
name: spec-check-drift
description: "Проверить, не устарела ли документация (спека/архитектура/C4/модель/глоссарий) относительно кода — и есть ли она вообще."
license: Apache-2.0
metadata:
  family: spec
  engine: codeintel
  tier: core
  mutates: false
---

# spec.check/drift

Проверить, не устарела ли документация (спека/архитектура/C4/модель/глоссарий) относительно кода — и есть ли она вообще.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "spec.check/drift", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap spec.check/drift <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `spec`, движок `codeintel`, тир `core`.
