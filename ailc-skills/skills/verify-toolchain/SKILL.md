---
name: verify-toolchain
description: "Узнать, какими средствами разработки и какими их версиями выполняется прогон: перечень инструментов стека с путями и версиями, а также честный перечень отсутствующих."
license: Apache-2.0
metadata:
  family: verify
  engine: runner
  tier: core
  mutates: false
---

# verify/toolchain

Узнать, какими средствами разработки и какими их версиями выполняется прогон: перечень инструментов стека с путями и версиями, а также честный перечень отсутствующих.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "verify/toolchain", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap verify/toolchain <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `verify`, движок `runner`, тир `core`.
