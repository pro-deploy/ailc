---
name: generate-doc
description: "Выпустить документ по стандарту целиком: техническое задание по ГОСТ 19.201-78 или ГОСТ 34.602-2020, описание архитектуры по arc42, диаграммы по модели C4. Пустой запрос выпускает весь комплект."
license: Apache-2.0
metadata:
  family: generate
  engine: generator
  tier: core
  mutates: true
---

# generate/doc

Выпустить документ по стандарту целиком: техническое задание по ГОСТ 19.201-78 или ГОСТ 34.602-2020, описание архитектуры по arc42, диаграммы по модели C4. Пустой запрос выпускает весь комплект.

## Как запустить

Через MCP-сервер ailc (один офлайновый бинарь, без внешних сервисов):

- `run { "id": "generate/doc", "path": "<путь к проекту>" }`
- семантический подбор под намерение: `find_capability { "query": "..." }`

Через CLI:

```
ailc cap generate/doc <путь>
```

Находки возвращаются структурно (file:line + severity) и проходят состязательную верификацию — в балл и гейт идут только подтверждённые. Семейство `generate`, движок `generator`, тир `core`.
