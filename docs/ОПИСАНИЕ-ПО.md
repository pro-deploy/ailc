# Описание программного обеспечения

<!-- ЭТОТ ДОКУМЕНТ СОБИРАЕТСЯ АВТОМАТИЧЕСКИ. Правки вносите ответами на
     вопросы (spec/answer) либо в зонах свободной прозы ailc:free. Всё
     остальное будет перезаписано при следующей сборке. -->

| Реквизит | Значение |
|---|---|
| Наименование изделия | ailc |
| Версия изделия | 0.7.0 |
| Обозначение документа | ОПО 59795 |
| Наименование документа | Описание программного обеспечения |
| Документ построен по | ГОСТ Р 59795-2021. Информационные технологии. Комплекс стандартов на автоматизированные системы. Автоматизированные системы. Требования к содержанию документов |
| Полнота по стандарту | 50 процентов (3 из 6 обязательных разделов) |
| Условия использования | Apache-2.0 |

## Содержание

- 0 Вводная часть (обязательный)
- 1 Структура программного обеспечения (обязательный)
- 2 Функции частей программного обеспечения (обязательный)
- 3 Методы и средства разработки программного обеспечения (обязательный)
- 4 Операционная система (обязательный)
- 5 Средства, расширяющие возможности операционной системы (обязательный)

## 0 Вводная часть

Сведения не определены. Чтобы закрыть раздел, ответьте на вопрос: Какие сведения о техническом, информационном и организационном обеспечении необходимо принять во внимание при разработке программного обеспечения, то есть в каком окружении оно создаётся и эксплуатируется, а также какими документами это окружение установлено? Ответ записывается вызовом `spec/answer` с идентификатором `опо.вводная-часть`.

Установлено из кода:

Наименование: ailc. Версия: 0.7.0. Условия использования: Apache-2.0. Состояние истории изменений: 9191ecb1eb6e.
<!-- ailc:free:start опо.вводная-часть -->
<!-- ailc:free:end -->

## 1 Структура программного обеспечения

- **crates/ailc-capabilities/src**: определений 1004, из них доступных извне 180. Среди них: fn insecure_output_scan, fn new, fn prompt_injection_scan, fn register, type LlmTaintCapability
- **crates/ailc-contracts/src**: определений 57, из них доступных извне 38. Среди них: type GatePolicy, type GateReport, type PolicyPack, type RunInput, type Thresholds
- **crates/ailc-core**: определений 1, из них доступных извне 0
- **crates/ailc-core/src**: определений 492, из них доступных извне 133. Среди них: fn get, fn run, fn set, fn today, type AgentOrchestrator
- **crates/ailc-core/src/engines**: определений 706, из них доступных извне 124. Среди них: fn extract, fn extract, fn is_empty, type ApiDoc, type ApiItem
- **crates/ailc-core/src/standards**: определений 29, из них доступных извне 20. Среди них: fn complete, fn completeness, fn draft_text, fn is_required, fn percent
- **crates/ailc-testkit/src**: определений 10, из них доступных извне 5. Среди них: fn ctx, fn new, fn path, fn write, type TempTree
- **crates/ailc/examples**: определений 16, из них доступных извне 0
- **crates/ailc/src**: определений 123, из них доступных извне 9. Среди них: fn install, fn run, fn run, fn run_once, fn uninstall
- **npm/ailc-mcp/bin**: определений 8, из них доступных извне 0
- **tools**: определений 7, из них доступных извне 7. Среди них: fn compact, fn main, fn preferred_id, fn ranges_of, fn severity_of
<!-- ailc:free:start опо.структура -->
<!-- ailc:free:end -->

## 2 Функции частей программного обеспечения

Сведения не определены. Чтобы закрыть раздел, ответьте на вопрос: Какую функцию выполняет каждая выделенная часть программного обеспечения, какие данные она принимает и возвращает, а также каким образом части взаимодействуют между собой при решении общей задачи? Ответ записывается вызовом `spec/answer` с идентификатором `опо.функции-частей`.

Установлено из кода:

- **AdrLifecycle** (тип): `pub struct AdrLifecycle`. Проверка журнала архитектурных решений: статусы и связи. Статусы и ссылки полезны ровно настолько, насколько им можно доверять. Запись без статуса, с выдуманным статусом, со ссылкой на несуществующее . Основано на: Capability, Default (crates/ailc-capabilities/src/workflow_extra.rs:638)
- **AgentPlan** (тип): `pub struct AgentPlan`. План агента — то, что нейросеть IDE возвращает на фазе PLAN. Заменяет keyword-роутер: ИИ сам решает, ЧТО запустить, строгая ли это «сдача» (`strict`), и можно ли чинить (`fix`). `stop_when` — критерий (crates/ailc-contracts/src/lib.rs:817)
- **Answer** (тип): `pub struct Answer`. Один ответ реестра. (crates/ailc-core/src/answers.rs:51)
- **AnswerSource** (перечисление): `pub enum AnswerSource`. Происхождение ответа. (crates/ailc-core/src/answers.rs:29)
- **Answers** (тип): `pub struct Answers`. Реестр ответов проекта. (crates/ailc-core/src/answers.rs:72)
- **ApiDoc** (тип): `pub struct ApiDoc;`. Движок извлечения описаний программных элементов. (crates/ailc-core/src/engines/apidoc.rs:107)
- **ApiEntry** (тип): `pub struct ApiEntry`. Объявление, доступное извне, вместе с его описанием. Перечень имён отвечает на вопрос «что есть», но документ по стандарту требует ответа на вопрос «что оно делает». Сигнатура и комментарий документац (crates/ailc-core/src/model.rs:174)
- **ApiItem** (тип): `pub struct ApiItem`. Описание одного программного элемента, пригодное для включения в раздел документа. (crates/ailc-core/src/engines/apidoc.rs:83)
- **Applicability** (перечисление): `pub enum Applicability`. Применимость вида документа к рассматриваемой системе. (crates/ailc-core/src/standards/rd50_34_698.rs:20)
- **AxisState** (тип): `pub struct AxisState`. Состояние одной оси проверки (например, оси дрейфа документации `spec.check/drift`) в агрегированном вердикте Волны 2: идентификатор оси, её исход и число прошедших верификацию находок по ней. Позволя (crates/ailc-contracts/src/lib.rs:755)
- **CallGraph** (тип): `pub struct CallGraph`. Граф вызовов: кто кого зовёт на уровне функций. Best-effort через AST: вызовы разрешаются по ИМЕНИ к определённым в проекте функциям; неразрешённые (stdlib, динамика, рефлексия) считаются ЯВНО — не мо (crates/ailc-core/src/engines/codeintel.rs:1536)
- **Capability** (интерфейс): `pub trait Capability: Send + Sync`. Инструмент = тонкая обёртка над движком + конфиг. Трейт намеренно узкий: вся общая логика живёт в движках (`engines::*`), а конкретный capability лишь отдаёт движку свою таблицу правил/шаблон. (crates/ailc-core/src/lib.rs:37)
- **CapabilityManifest** (тип): `pub struct CapabilityManifest`. Декларативное описание инструмента для реестра и Capability Router. (crates/ailc-contracts/src/lib.rs:789)
- **CapabilityOutput** (тип): `pub struct CapabilityOutput`. Единый выход capability. `skipped` — инвариант «нет молчаливых пропусков»: если шаг не выполнен, тут причина. `records` — обобщённые текстовые записи (символы, узлы графа и т.п.) до подключения типизи (crates/ailc-contracts/src/lib.rs:654)
- **Certainty** (перечисление): `pub enum Certainty`. Уверенность в факте. Повторяет контрактную шкалу, но пригодна к сериализации.. Основано на: From (crates/ailc-core/src/model.rs:46)
- **ChangeClass** (перечисление): `pub enum ChangeClass`. Класс изменения. Определяет, какие документы обязаны сопровождать правку. (crates/ailc-core/src/changes.rs:26)
- **ChangeLog** (тип): `pub struct ChangeLog`. Запись изменения в журнал. Обязательство «документируется любое, даже мелкое изменение» исполняется здесь и исполняется сервером: запись формируется детерминированно из самого различия, класс изменени. Основано на: Capability, Default (crates/ailc-capabilities/src/docs_std.rs:153)
- **ChangeRecord** (тип): `pub struct ChangeRecord`. Запись журнала изменений. (crates/ailc-core/src/changes.rs:71)
- **CheckOutcome** (перечисление): `pub enum CheckOutcome`. Исход одной проверки (оси) для агрегатора Волны 2. Различает «не выполнялось» от «выполнено, находок нет»: раньше эти случаи неразличимо сливались, из-за чего сбой инструмента превращался в находку, а (crates/ailc-contracts/src/lib.rs:724)
- **CliItem** (тип): `pub struct CliItem`. Один извлечённый факт командного интерфейса или конфигурации: его значение и место, где он объявлен, чтобы из документа можно было перейти к исходному тексту. (crates/ailc-core/src/engines/cli.rs:31)
- **CliSurface** (тип): `pub struct CliSurface`. Командный интерфейс проекта и состав его конфигурации. (crates/ailc-core/src/engines/cli.rs:39)
- **Completeness** (тип): `pub struct Completeness`. Итог оценки полноты документа по стандарту. (crates/ailc-core/src/standards/mod.rs:253)
- **Condition** (перечисление): `pub enum Condition`. Условие, при котором раздел становится обязательным. (crates/ailc-core/src/standards/mod.rs:83)
- **Confidence** (перечисление): `pub enum Confidence`. Уверенность детектора — ОТДЕЛЬНО от severity. severity = «насколько плохо, если правда»; confidence = «насколько уверены, что это правда». Как у Semgrep (`metadata.confidence`): точные токены / структ. Основано на: fmt::Display (crates/ailc-contracts/src/lib.rs:143)
- **Ctx** (тип): `pub struct Ctx`. Контекст исполнения. (crates/ailc-contracts/src/lib.rs:872)
- **DepGraph** (тип): `pub struct DepGraph`. Граф зависимостей модулей. Циклы = SCC размером > 1. Узлы — полные относительные пути папок-пакетов (T65). Структура сохранена в прежней форме (только `modules` и `edges`), чтобы существующие вызывающ (crates/ailc-core/src/engines/codeintel.rs:699)
- **Dependency** (тип): `pub struct Dependency`. Зависимость поставки с версией. (crates/ailc-core/src/model.rs:232)
- **Deployment** (тип): `pub struct Deployment`. Развёртывание: где и как система исполняется. (crates/ailc-core/src/model.rs:240)
- **DepsAudit** (тип): `pub struct DepsAudit`. security.scan/deps — реальный аудит зависимостей внешним инструментом. Недетерминированно: зависит от установленного тулчейна и базы уязвимостей. Кормит гейт: при найденных уязвимостях эмитит Finding . Основано на: Capability, Default (crates/ailc-capabilities/src/security_extra.rs:306)
- **Diff** (тип): `pub struct Diff`. Итог разбора различия рабочего дерева. (crates/ailc-core/src/changes.rs:190)
- **DirStat** (тип): `pub struct DirStat`. Статистика по одной папке. (crates/ailc-core/src/engines/codeintel.rs:611)
- **DocKind** (тип): `pub struct DocKind`. Вид документа комплекта. (crates/ailc-core/src/standards/rd50_34_698.rs:31)
- **Document** (тип): `pub struct Document`. Документ в декларации стандарта. (crates/ailc-core/src/standards/mod.rs:215)
- **Draft** (перечисление): `pub enum Draft`. Правило построения содержания либо черновика раздела из модели проекта. Черновик существенно меняет характер опроса: агент спрашивает не с чистого листа, а на подтверждение уже выведенного из кода, и  (crates/ailc-core/src/standards/mod.rs:152)
- **EngineKind** (перечисление): `pub enum EngineKind`. Девять переиспользуемых движков. Capability = тонкий конфиг поверх одного из них.. Основано на: fmt::Display (crates/ailc-contracts/src/lib.rs:69)
- **Environment** (тип): `pub struct Environment`. Технологическое окружение: чем система написана и чем собирается. (crates/ailc-core/src/model.rs:150)
- **Fact** (тип): `pub struct Fact<T>`. Значение вместе с происхождением. (crates/ailc-core/src/model.rs:114)
- **Family** (перечисление): `pub enum Family`. Семейство capability (см. карту консолидации 01-CONSOLIDATION.md).. Основано на: fmt::Display (crates/ailc-contracts/src/lib.rs:32)
- **FileMetric** (тип): `pub struct FileMetric`. Метрика одного файла: путь относительно корня, число строк, сложности и глубина. (crates/ailc-core/src/engines/metric.rs:13)
- **Finding** (тип): `pub struct Finding`. Находка. `verified` и `evidence` — несущая стена анти-гейминга: очки начисляются только за находку, пережившую verify и заземлённую на file:line. (crates/ailc-contracts/src/lib.rs:168)
- **Gap** (тип): `pub struct Gap`. Неустановленное сведение: что именно не удалось выяснить и почему. (crates/ailc-core/src/model.rs:128)
- **GatePolicy** (тип): `pub struct GatePolicy`. Политика гейта — часть PolicyPack (governance как ДАННЫЕ, авторит старший). `block_at` — severity, начиная с которой находка блокирует. `families` — какие семейства проверок гонять (пусто = все check-. Основано на: Default (crates/ailc-contracts/src/lib.rs:917)
- **GateReport** (тип): `pub struct GateReport`. Вердикт гейта — граница ответственности. Структурный контракт (не проза): blocking vs warning, балл качества, и ЯВНЫЙ список пропущенных проверок (инвариант «нет молчаливых пропусков»). (crates/ailc-contracts/src/lib.rs:999)
- **GenerateAdr** (тип): `pub struct GenerateAdr`. Запись архитектурного решения (ADR) отдельным файлом в журнал решений.. Основано на: Capability, Default (crates/ailc-capabilities/src/workflow_extra.rs:30)
- **Hit** (тип): `pub struct Hit`. Один результат поиска по индексу. (crates/ailc-core/src/engines/index.rs:68)
- **Identity** (тип): `pub struct Identity`. Идентификация продукта: то, чем документ подписывается на титульном листе. (crates/ailc-core/src/model.rs:137)
- **LexicalIndex** (тип): `pub struct LexicalIndex`. Обратный индекс терминов по дереву исходников вместе со сведениями, необходимыми формуле BM25 (частоты, длины документов, средняя длина документа). Все внутренние коллекции упорядоченные. Это не стили (crates/ailc-core/src/engines/index.rs:87)
- **LlmTaintCapability** (тип): `pub struct LlmTaintCapability`. Лёгкий внутрифайловый taint-анализ вывода LLM (OWASP LLM02, CWE-94/CWE-79). Зачем отдельная capability, а не таблица правил. Построчные правила [`insecure_output_scan`] требуют имя-маркер вывода (resp. Основано на: Capability, Default (crates/ailc-capabilities/src/ai_security.rs:246)
- **Matcher** (перечисление): `pub enum Matcher`. Как правило решает, сработала ли строка (или фрагмент из нескольких строк). Один движок переиспользуется всеми сканерами (secret/owasp/pii/iac/…), поэтому и набор способов матча живёт здесь, а не в ка (crates/ailc-core/src/engines/scan.rs:42)
- **Module** (тип): `pub struct Module`. Модуль системы как единица состава. (crates/ailc-core/src/model.rs:160)
- **ModuleStat** (тип): `pub struct ModuleStat`. Сводка по одной части проекта. (crates/ailc-core/src/engines/codeintel.rs:602)
- **Origin** (тип): `pub struct Origin`. Происхождение факта: чем установлен и где это видно. (crates/ailc-core/src/model.rs:68)
- **OsvReport** (тип): `pub struct OsvReport`. Результат нативной проверки. (crates/ailc-core/src/engines/osv.rs:136)
- **OutboundCall** (тип): `pub struct OutboundCall`. Исходящий сетевой вызов из модуля наружу (URL/gRPC/клиент внешнего сервиса). Основа межсервисного графа (T67): из них строится C4-Container, в отличие от прежнего подхода, где контейнерами объявлялись (crates/ailc-core/src/engines/codeintel.rs:680)
- **PdnLogsAst** (тип): `pub struct PdnLogsAst`. compliance.ru/pdn-logs-ast — ПДн в логах СТРУКТУРНО (AST). Дополняет line-regex `pdn-logs` тем, что regex принципиально не умеет: многострочные вызовы (аргумент на другой строке, глубоко разнесённый п. Основано на: Capability, Default (crates/ailc-capabilities/src/compliance.rs:260)
- **PlanStep** (тип): `pub struct PlanStep`. Один шаг плана агента: какой инструмент запустить и зачем (человеческим языком). (crates/ailc-contracts/src/lib.rs:805)
- **PolicyPack** (тип): `pub struct PolicyPack`. PolicyPack — governance как ДАННЫЕ. Авторит старший один раз; джун наследует, ничего не выбирая. Загружается из `ailc.policy.toml` в корне проекта. Несёт политику гейта и пороги качества; дальше — con. Основано на: Default (crates/ailc-contracts/src/lib.rs:979)
- **ProjectMap** (тип): `pub struct ProjectMap`. Карта проекта целиком. (crates/ailc-core/src/engines/codeintel.rs:620)
- **ProjectModel** (тип): `pub struct ProjectModel`. Полная модель проекта. (crates/ailc-core/src/model.rs:247)
- **ProjectProfile** (тип): `pub struct ProjectProfile`. Детерминированный паспорт проекта. (crates/ailc-core/src/profile.rs:26)
- **QualityLedger** (тип): `pub struct QualityLedger`. QualityLedger — ЛИЦО продукта для не-эксперта. Итог прогона пайплайна на человеческом языке: что проверили, что нашли, балл качества, и какие РЕШЕНИЯ нужны от человека (эскалация простыми словами, без (crates/ailc-contracts/src/lib.rs:832)
- **Recorded** (тип): `pub struct Recorded`. Итог фиксации базовой линии: сколько отпечатков заморожено и сколько находок заморозить отказано (уязвимости, см. [`is_freezable`]). (crates/ailc-core/src/baseline.rs:99)
- **ReferenceHits** (тип): `pub struct ReferenceHits`. Результат поиска ссылок с учётом усечения (T70). (crates/ailc-core/src/engines/codeintel.rs:1101)
- **RenderedDocument** (тип): `pub struct RenderedDocument`. Собранный документ вместе с оценкой его полноты по стандарту. (crates/ailc-core/src/engines/docgen.rs:46)
- **RepoScale** (тип): `pub struct RepoScale`. Наблюдаемые характеристики репозитория, по которым ведётся калибровка. (crates/ailc-core/src/calibrate.rs:27)
- **Requirement** (перечисление): `pub enum Requirement`. Обязательность раздела по стандарту. (crates/ailc-core/src/standards/mod.rs:121)
- **Rule** (тип): `pub struct Rule`. Одно правило сканера: какие расширения, как матчить строку, что эмитить. (crates/ailc-core/src/engines/scan.rs:272)
- **RuleAdd** (тип): `pub struct RuleAdd`. `governance/rule-add` — закон одной репликой. Принимает готовую строку правила (`FORBID …` / `REQUIRE …` / `REQUIRE_EACH …`, атрибуты `[warn]` и `[in: путь]` опциональны), дописывает её в раздел «Прав. Основано на: Capability, Default (crates/ailc-capabilities/src/governance.rs:759)
- **RuleConfidence** (перечисление): `pub enum RuleConfidence`. Класс достоверности правила: атрибут самого детектора, а не внешний список. `Heuristic` (строка/стиль/метрика/совет) переходит в Low; `Pattern` (надёжное структурное/паттерн-совпадение, но не уникальн (crates/ailc-contracts/src/lib.rs:230)
- **RunInput** (тип): `pub struct RunInput`. Вход capability (минимальный; расширяется по семействам). (crates/ailc-contracts/src/lib.rs:905)
- **Sampler** (интерфейс): `pub trait Sampler`. Доступ к LLM (модель клиента через MCP sampling). Реализуется транспортом (бинарём), ядро от транспорта не зависит. Используется агентом и автофиксом. (crates/ailc-core/src/orchestrator.rs:33)
- **SastScan** (тип): `pub struct SastScan`. security.scan/sast — структурный анализ безопасности по абстрактному синтаксическому дереву (AST). Помечен `Tier::Enterprise`, и это сознательное решение, согласованное со слоем детерминированных вход. Основано на: Capability, Default (crates/ailc-capabilities/src/security_extra.rs:502)
- **ScanCapability** (тип): `pub struct ScanCapability`. Любой сканер = манифест + таблица правил поверх `ScanEngine`. Один `impl Capability` обслуживает их все — нулевое дублирование boilerplate.. Основано на: Capability (crates/ailc-capabilities/src/lib.rs:90)
- **ScanReport** (тип): `pub struct ScanReport`. Результат сплошного статического скана: подтверждённые находки (ложные отсеяны), сколько опровергнуто Verifier'ом, и какие проверки выполнены/пропущены. (crates/ailc-core/src/orchestrator.rs:418)
- **Section** (тип): `pub struct Section`. Раздел документа в декларации стандарта. (crates/ailc-core/src/standards/mod.rs:198)
- **ServiceGraph** (тип): `pub struct ServiceGraph`. Межсервисный взгляд (T67): исходящие сетевые вызовы и развёртываемые контейнеры. Отдельно от [`DepGraph`], чтобы добавление было строго аддитивным. (crates/ailc-core/src/engines/codeintel.rs:709)
- **Size** (перечисление): `pub enum Size`. Класс размера кодовой базы. Границы намеренно грубые: точность здесь не нужна, нужна устойчивость решения к добавлению десятка файлов. (crates/ailc-core/src/calibrate.rs:53)
- **SkillFile** (тип): `pub struct SkillFile`. Один файл будущего пака: относительный путь + содержимое. (crates/ailc-core/src/skills.rs:14)
- **Source** (перечисление): `pub enum Source`. Откуда берётся содержание раздела. (crates/ailc-core/src/standards/mod.rs:134)
- **Standard** (перечисление): `pub enum Standard`. Стандарт или признанная практика, по которой построен документ. (crates/ailc-core/src/standards/mod.rs:40)

Всего объявлений, доступных извне: 516; выше приведены первые восемьдесят.
<!-- ailc:free:start опо.функции-частей -->
<!-- ailc:free:end -->

## 3 Методы и средства разработки программного обеспечения

Технологический стек:
- `Rust`

Зафиксированные версии инструментария:
- `Rust 1.93.1` (rust-toolchain.toml)
<!-- ailc:free:start опо.методы-и-средства-разработки -->
<!-- ailc:free:end -->

## 4 Операционная система

Сведения не определены. Чтобы закрыть раздел, ответьте на вопрос: Под управлением какой операционной системы и какой её версии работает программное обеспечение, а также какие требования предъявляются к её настройке, разрядности и набору установленных системных компонентов? Ответ записывается вызовом `spec/answer` с идентификатором `опо.операционная-система`.

Установлено из кода:

Технологический стек:
- `Rust`

Зафиксированные версии инструментария:
- `Rust 1.93.1` (rust-toolchain.toml)
<!-- ailc:free:start опо.операционная-система -->
<!-- ailc:free:end -->

## 5 Средства, расширяющие возможности операционной системы

| Экосистема | Наименование | Версия |
|---|---|---|
| crates.io | adler2 | 2.0.1 |
| crates.io | aho-corasick | 1.1.4 |
| crates.io | ailc | 0.7.0 |
| crates.io | ailc-capabilities | 0.7.0 |
| crates.io | ailc-contracts | 0.7.0 |
| crates.io | ailc-core | 0.7.0 |
| crates.io | ailc-testkit | 0.7.0 |
| crates.io | cc | 1.2.65 |
| crates.io | cfg-if | 1.0.4 |
| crates.io | crc32fast | 1.5.0 |
| crates.io | equivalent | 1.0.2 |
| crates.io | find-msvc-tools | 0.1.9 |
| crates.io | flate2 | 1.1.9 |
| crates.io | hashbrown | 0.17.1 |
| crates.io | indexmap | 2.14.0 |
| crates.io | itoa | 1.0.18 |
| crates.io | memchr | 2.8.2 |
| crates.io | miniz_oxide | 0.8.9 |
| crates.io | proc-macro2 | 1.0.106 |
| crates.io | quote | 1.0.46 |
| crates.io | regex | 1.12.4 |
| crates.io | regex-automata | 0.4.14 |
| crates.io | regex-syntax | 0.8.11 |
| crates.io | serde | 1.0.228 |
| crates.io | serde_core | 1.0.228 |
| crates.io | serde_derive | 1.0.228 |
| crates.io | serde_json | 1.0.150 |
| crates.io | serde_spanned | 0.6.9 |
| crates.io | shlex | 2.0.1 |
| crates.io | simd-adler32 | 0.3.10 |
| crates.io | streaming-iterator | 0.1.9 |
| crates.io | syn | 2.0.118 |
| crates.io | toml | 0.8.23 |
| crates.io | toml_datetime | 0.6.11 |
| crates.io | toml_edit | 0.22.27 |
| crates.io | toml_write | 0.1.2 |
| crates.io | tree-sitter | 0.25.10 |
| crates.io | tree-sitter-c | 0.23.4 |
| crates.io | tree-sitter-c-sharp | 0.23.5 |
| crates.io | tree-sitter-cpp | 0.23.4 |
| crates.io | tree-sitter-dart | 0.2.0 |
| crates.io | tree-sitter-go | 0.23.4 |
| crates.io | tree-sitter-java | 0.23.5 |
| crates.io | tree-sitter-javascript | 0.23.1 |
| crates.io | tree-sitter-kotlin-ng | 1.1.0 |
| crates.io | tree-sitter-language | 0.1.7 |
| crates.io | tree-sitter-php | 0.23.11 |
| crates.io | tree-sitter-python | 0.23.6 |
| crates.io | tree-sitter-ruby | 0.23.1 |
| crates.io | tree-sitter-rust | 0.23.3 |
| crates.io | tree-sitter-scala | 0.23.4 |
| crates.io | tree-sitter-swift | 0.7.3 |
| crates.io | tree-sitter-typescript | 0.23.2 |
| crates.io | unicode-ident | 1.0.24 |
| crates.io | winnow | 0.7.15 |
| crates.io | zmij | 1.0.21 |
<!-- ailc:free:start опо.средства-расширения-ос -->
<!-- ailc:free:end -->

## Лист регистрации изменений

| Изменение | Дата | Класс | Затронуто файлов | Основание |
|---|---|---|---|---|
| 1 | 2026-07-28 | слом публичного контракта | 228 | main |

## Отметка о полноте

Закрыто 3 из 6 обязательных разделов (50 процентов). До полного соответствия требованиям к составу разделов не закрыты:

- 0 Вводная часть
- 2 Функции частей программного обеспечения
- 4 Операционная система

Очередь вопросов по незакрытым разделам выдаёт `spec/questions`.
